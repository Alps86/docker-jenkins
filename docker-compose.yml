version: '2'
networks:
  default:
    external:
      name: nginx-proxy

services:
  drupal:
      image: drupal
      container_name: drupal-${JOB_BASE_NAME}
      environment:
        - VIRTUAL_HOST=${JOB_BASE_NAME}.drupal.localhost