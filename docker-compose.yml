version: '2'
networks:
  default:
    external:
      name: nginx-proxy

services:
  drupal:
      image: wordpress
      container_name: drupal.${JOB_BASE_NAME}.localhost
      environment:
        - VIRTUAL_HOST=drupal.${JOB_BASE_NAME}.localhost